{
  "name": "prysmi-website",
  "version": "1.0.0",
  "description": "PRYSMI Agency website with Tailwind CSS build process",
  "main": "index.html",
  "scripts": {
    "build-css": "tailwindcss -i ./src/input.css -o ./dist/style.css --minify",
    "watch-css": "tailwindcss -i ./src/input.css -o ./dist/style.css --watch",
    "build-three": "vite build --outDir temp && mkdir -p dist/assets/js && cp temp/assets/*.js dist/assets/js/three-optimized.js",
    "build": "npm run build-css && npm run build-three && mkdir -p dist && cp index.html dist/ && cp -r assets dist/ && cp -r public/* dist/",
    "dev": "vite",
    "start": "vite"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0",
    "vite": "^5.0.0"
  },
  "dependencies": {
    "three": "^0.167.1"
  },
  "keywords": [
    "tailwind",
    "css",
    "b2b",
    "marketing",
    "website"
  ],
  "author": "PRYSMI Agency",
  "license": "ISC"
}
