{
  "name": "prysmi-website",
  "version": "1.0.0",
  "description": "PRYSMI Agency website with Tailwind CSS build process",
  "main": "index.html",
  "scripts": {
    "clean": "rimraf dist",
    "build": "npm run clean && npm run build:css && npm run build:js && npm run copy && npm run inline:css",
    "build:css": "tailwindcss -i ./src/input.css -o ./dist/style.css --minify",
    "build:js": "rollup -c",
    "copy": "copyfiles -u 1 \"assets/**/*\" dist/assets && copyfiles index.html dist && copyfiles -f public/* dist && copyfiles -R functions dist",
    "inline:css": "node inline-css.js",
    "dev": "live-server dist --watch=dist & npm run watch:css",
    "watch:css": "tailwindcss -i ./src/input.css -o ./dist/style.css --watch"
  },
  "devDependencies": {
    "@rollup/plugin-node-resolve": "^16.0.1",
    "autoprefixer": "^10.4.21",
    "copyfiles": "^2.4.1",
    "live-server": "^1.2.2",
    "postcss": "^8.5.6",
    "rimraf": "^6.0.1",
    "rollup": "^2.79.2",
    "rollup-plugin-terser": "^7.0.2",
    "tailwindcss": "^3.4.17",
    "three": "^0.179.1"
  },
  "keywords": ["tailwind", "css", "b2b", "marketing", "website"],
  "author": "PRYSMI Agency",
  "license": "ISC"
}
